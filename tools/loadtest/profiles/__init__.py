"""Workload profile runners (plan section 5): A (baseline) and B (ramp) are
the two fully-implemented, exhaustively-correct profiles per the Phase 3
task's priority ordering. C/D/E/F are callable but intentionally reduced in
scope -- see each module's docstring for exactly what's covered."""
